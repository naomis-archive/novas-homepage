import { IconDefinition } from "@fortawesome/fontawesome-svg-core";

export interface Social {
  name: string;
  icon: IconDefinition;
  url: string;
  color: string;
  background: string;
  hover: boolean;
}
